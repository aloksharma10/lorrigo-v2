{
  "name": "@lorrigo/db",
  "description": "Database package for Lorrigo",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p .",
    "db:generate": "npx prisma generate --schema=./prisma/schema",
    "db:migrate": "prisma migrate dev --schema=./prisma/schema --skip-generate",
    "db:deploy": "prisma migrate deploy --schema=./prisma/schema",
    "studio": "npx prisma studio --schema=./prisma/schema --browser none"
  },
  "author": "<aloksharma.cs1@gmail.com> Alok Sharma",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20",
    "prisma": "^6.7.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "6.7.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}